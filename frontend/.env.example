VITE_API_URL=http://localhost:8000

# Optional: override the GitHub link rendered on the landing page.
# Falls back to the upstream repo if unset.
# VITE_GITHUB_URL=https://github.com/your-org/your-fork

# Auth (keep false for local-dev; pair with backend AUTH_ENABLED)
VITE_AUTH_ENABLED=false
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
# VITE_FIREBASE_EMULATOR=true
